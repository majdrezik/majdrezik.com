[build]
  publish = "."

[[headers]]
  for = "/update.json"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"
    Access-Control-Allow-Origin = "*"

[[headers]]
  for = "/bundles/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
